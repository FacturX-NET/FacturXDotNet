/**
 * Code list for the format of a date, when the date only contains the day, month and year.
 *
 * @remarks This enumeration only has one value because there is only one possible code when the date only contains the day, month and year. However, this enumeration is still useful to
 * make the code more readable.
 */
export type DateOnlyFormat = '102-date-only';
